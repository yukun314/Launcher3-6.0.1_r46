<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:clipToPadding="true"
    android:background="@color/setting_bg_color">

    <include
        android:id="@+id/activity_help_feedback_navigation"
        layout="@layout/activity_navigation"/>

    <TextView
        android:id="@+id/activity_help_feedback_bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:textSize="20sp"
        android:textColor="#fff"
        android:gravity="center"
        android:padding="10dp"
        android:text="@string/setting_feedback"/>

    <WebView
        android:id="@+id/activity_help_feedback_webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hardwareAccelerated="false"
        android:layout_below="@id/activity_help_feedback_navigation"
        android:layout_above="@id/activity_help_feedback_bottom"
        android:background="#00000000"/>

</RelativeLayout>